// Deprecated legacy history module
export const addHistory = (record) => {
  return record;
}

export const getHistory = (userId = null) => {
  return [];
}

export const getHistoryByShipment = (shipmentId, userId = null) => {
  return [];
}
