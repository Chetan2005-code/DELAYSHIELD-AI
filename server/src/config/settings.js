export const systemSettings = {
  // Global flag to enable or disable the Unified Demo Scenario Framework
  demoMode: process.env.DEMO_MODE !== 'false' // defaults to true unless explicitly disabled
};
